package reversiassignment;

public class MiniMax {
	private GameState currentState;
	
	public MiniMax(GameState s) {
		currentState = s;
	}
	
	public Position miniMaxDecision(GameState state) {
		return null;
	}
	
	public int maxValue(GameState state) {
		/**
		 * If terminal-test(state) return utility
		 * for all successor states do v< max(c,min-value(s))
		 **/
		return 0;
	}
	
	private int minValue(GameState state){
		return 0;
	}
}
