package reversiassignment;

import java.util.LinkedList;
import java.util.List;

public class GameState {
	Board board;
	int score;
	Position move;
	
	public GameState(Board b, int s, Position p) {
		board = b;
		score = s;
		move = p;
	}
	
	public void addChild(GameState child) {
		children.add(child);
	}
}
